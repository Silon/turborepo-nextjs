import { useContext } from "react";
import { {{pascalCase name}}ActionsContext, {{pascalCase name}}StateContext } from "./{{pascalCase name}}.context";

export function use{{pascalCase name}}State() {
  const context = useContext({{pascalCase name}}StateContext);
  if (context === undefined) {
    throw new Error("use{{pascalCase name}}StateContext must be used within a {{pascalCase name}}Provider");
  }

  return context;
}

export function use{{pascalCase name}}Actions() {
  const context = useContext({{pascalCase name}}ActionsContext);
  if (context === undefined) {
    throw new Error("use{{pascalCase name}}ActionsContext must be used within a {{pascalCase name}}Provider");
  }

  return context;
}
